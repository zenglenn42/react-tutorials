import React from 'react'
import { div } from '@material-ui/core'

const Home = (props) => {
    const {className} = props
    return (
        <div className={className}>
            home page
        </div>
    )
}

export default Home
