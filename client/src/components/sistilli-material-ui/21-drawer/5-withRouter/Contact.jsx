import React from 'react'

const Contact = (props) => {
    const {className} = props
    return (
        <div className={className}>
            contact page
        </div>
    )
}

export default Contact
